import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>EduPortal-CET</title>
      </Head>
    </div>
  );
};

export default Home;
